<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DynamicWallpaperApplication" parent="Theme.MaterialComponents.NoActionBar.Bridge">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:navigationBarColor">@color/deep_white</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item> <!-- 使状态栏文本变为深色 -->
    </style>

    <style name="Theme.DynamicWallpaperApplication" parent="Base.Theme.DynamicWallpaperApplication" />
</resources>